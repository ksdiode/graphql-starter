type Todo {
  id: ID!
  title: String!
  done: Boolean
}

input TodoInput {
  title: String
  done: Boolean
}

type Query {
  getTodoList: [Todo]
  getTodo(id: ID!): Todo
}

type Mutation {
  addTodo(todo: TodoInput): Todo
  removeTodo(id: ID!): Todo
}
