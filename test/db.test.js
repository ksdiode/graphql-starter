import { dotenv } from 'dotenv';
dotenv.config();
// import connectDB from '../database';

describe('데이터베이스', () => {
  beforeAll(async () => {
    console.log('---------------');
    // await connectDB();
  });
});
